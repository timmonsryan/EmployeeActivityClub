<h1>Edit Post</h1>
<%= form_for(@post) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :body %>
	<%= f.text_area :body, rows: 20, id: "postBodyField" %>

	<%= f.submit "Update Post" %>
<% end %>